<%= form_with(model: vehicle, local: true) do |form| %>
  <% if vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
      <% vehicle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :chassi %>
    <%= form.text_field :chassi, id: :vehicle_chassi %>
  </div>

  <div class="field">
    <%= form.label :placa %>
    <%= form.text_field :placa, id: :vehicle_placa %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :vehicle_description %>
  </div>

  <div class="field">
    <%= form.label :office_id %>
    <%= form.text_field :office_id, id: :vehicle_office_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
